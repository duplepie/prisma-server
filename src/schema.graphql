type Query {
  coin(coinId: String!): Coin!
  coins: [Coin!]!
  searchCoin(text: String!): [Coin!]!
  syncCoins: [Coin!]!
}

type Mutation {
  addCoin(id: String!, symbol: String!, name: String!): Coin!
}

type Coin {
  id: ID!
  coinId: String!
  name: String!
  symbol: String!
}